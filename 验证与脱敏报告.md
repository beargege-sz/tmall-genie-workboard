# 验证与脱敏报告
日期：2026-09-18。此报告针对本公开包，不是对其他设备的验收证明。

## 已验证
- 示例依赖可安装；Node实际验证版本24.11.0（文档要求22+兼容版本）。
- 18项测试通过，含周记解析、额度桶、过期快照、照片比例、字幕和独立HTTP服务。
- 独立服务测试：/health 200；未授权快照与图片401；带令牌快照200；错误Webhook令牌401；正确ShowBoard返回指定模板；/.env 404。
- 示例额度默认为--，虚构周记显示6条未完成事项。
- 公开ZIP已解压到独立临时目录，重新npm ci并运行18项测试全部通过；构建缓存已从ZIP移除。
- Waft锁定版本构建成功，app.wasm 118236字节，WebAssembly.validate=true。
- 宿主导入检查未发现此前不兼容的env.seed或dispatch_native_call_sync2。
- 源代码经过真实用户名、域名、IP、模板标识、设备/应用ID、私钥头及已知令牌片段扫描；发现的历史测试模板标识已替换。
- 分享ZIP仅含白名单文档、源码、测试、锁文件、空配置模板和虚构周记，不包含运行凭证、照片、二进制包或安装依赖。

## 未验证及风险
- 没有拿另一台天猫精灵为公开包重新做设备验收；历史真机成功来自原项目。
- 未对当前所有平台账号权限、旧入口持续有效性作保证。
- 公共示例服务经过独立HTTP测试，未取代线上服务；本次没有变更现有线上系统。
- 本包不是全面安全审计。旧Waft依赖安装时有deprecated提示，其中包含loadsh包被标记为typosquat的警告，以及过时router/glob等。
- 为复现保留原锁版本，并使用npm ci --ignore-scripts；这不能消除导入或执行依赖时的风险。建议在隔离开发环境构建，不让构建环境持有生产密钥；正式使用前独立审计依赖，不能自动升级后假定设备仍兼容。
- 不附预编译WASM；在自己的环境检查依赖、构建日志和产物后再上传。
- 完成勾选、设备授权、长时常驻及原生图片预加载仍未实现。

## 分享范围
只分享本目录或它的公开ZIP。不要把上层项目docs、.env、data、证书包、日志和历史截图附进去。
屏幕录像需要另行审查，源码脱敏并不代表录像已脱敏。
