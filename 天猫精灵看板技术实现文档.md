# 天猫精灵看板技术实现文档
## 从语音技能到照片、额度和周记待办的双端工作台

版本：1.0 · 2026-09-18 · 公开脱敏版
适用读者：希望把带屏天猫精灵改造成个人信息看板，并用 Codex 或其他开发工具协助实现的人。

本文是一份真实项目复盘和可复现工程指南，不是阿里官方兼容性承诺。平台入口、权限及固件能力可能变化。先做最小真机验证，再投入完整开发。

## 1. 最终实现了什么，尚未实现什么

本案例把一台天猫精灵 CCL（型号 TG_S2A，记录固件 6.1.8-RS-20221104.1459）作为屏幕：
左上显示周记照片，保持原图比例；右侧显示主 Codex 周额度及重置时间；右上显示北京时间；底部三列四行显示最多12条下周待办。
照片按洗牌队列每3–5分钟切换；快照每分钟更新；额度采集每5分钟一次。
电脑浏览器提供响应式 Web 版本，窄屏改为上下排列。

已经有目标设备文字和照片正常显示的用户反馈，有线上接口和浏览器验证。
**未完成的能力**：触屏打勾、跨端完成状态、账户登录/设备授权、开机自动进入、无限常驻、完整息屏恢复验收、自动续证、局域网HTTPS直连。
公开包是在已运行项目基础上整理的教学版本，并未在其他型号重新验收。

本方案无需刷机、USB、ADB、内置浏览器、支付宝小程序、快链应用或安卓APK。
这不意味着这些路线不可用，只是本案例最终没有使用它们。不能因为控制台有某入口就认定自己的账号和设备获准使用。

## 2. 架构：同一份数据，两套界面

```text
iCloud已下载周记 / 照片       本机已登录的Codex
           ↓ 只读                  ↓ 只读额度
          常驻Mac mini：Node服务 + 快照 + 缩略图
                           ↑
                  反向SSH隧道（仅回环端口）
                           ↑
                 云服务器Nginx + 公网HTTPS
                  ↑                       ↑
       Web浏览器读取快照          AliGenie调用Webhook
                                        ↓
                              Speak + RenderTemplate
                                        ↓
                           天猫精灵Waft页面读取同份快照
```

- 普通网页：HTML/CSS/JavaScript，浏览器渲染。
- 设备页面：Waft的AXML/ACSS/TypeScript风格源码，编译为WASM，设备引擎渲染。
- 不能把HTML文件直接作为屏显包上传，也不能把Web预览当作设备验收。
- Mac mini和天猫精灵虽在同一个局域网，当前案例仍经公网HTTPS。云端承担入口、TLS终止和转发，照片会经过该服务器，但无需把照片库整体搬到云盘。
- 局域网优化要另做域名解析、可信证书、设备DNS可达性和监听边界设计；平台Webhook通常仍需要平台可访问的入口。不要为省中转改为明文传私人数据。

## 3. 开始之前准备什么

| 类别 | 必备内容 | 检查方法 |
| --- | --- | --- |
| 设备 | 带屏天猫精灵、型号、固件、正常联网 | 拍“设备信息”，分享时遮挡UUID、MAC、SN |
| 账号 | AliGenie开发者登录及个人技能/屏显可用权限 | 能创建技能、看到屏显模板和测试入口 |
| 测试关系 | 开发者账号设备或语音验证码绑定 | 真机测试开关开启，设备已列出 |
| 主机 | 持续运行的电脑；本案例为Mac mini | 用户登录后后台服务可读资料；睡眠会影响服务 |
| 运行环境 | 示例建议Node22+、npm、SSH；Waft锁版本 | node -v、安装依赖、最小构建 |
| 数据 | 测试周记和无隐私照片，再接个人资料 | iCloud文件需本地可读，不能只有占位 |
| 公网 | 自己控制的域名、服务器/等价HTTPS入口 | DNS、443可达、完整可信证书链 |
| 密钥 | 查看令牌、Webhook令牌、独立隧道SSH密钥 | 三者隔离；不放源码、图片字幕或录像 |
| Codex（可选） | 常驻主机以预期账号自行登录Codex | 只采集主账号周窗口；缺失时显示未知 |

不要让AI代你猜域名、路径、证书、账号权限。不要购买额外硬件或证书，直到最小路线确认有必要。
证书是否收费不是兼容性的判断依据；核心是具体链是否被调用方信任。域名合规与技能资质以部署区域和平台当前要求为准。

## 4. 推荐实施顺序：每步都有验收门

### 4.1 先跑本地Web，不接平台

使用公开包 example，按README创建.env并配置两个不同随机令牌。示例服务只监听127.0.0.1。
启动命令是 `node --env-file=.env src/server.js`；不能只创建.env却忘记加载。
默认包含虚构周记；额度为空。手动放一张已获授权的JPEG到示例photos文件夹。

浏览器访问 `http://127.0.0.1:8787/?view=<自己的查看令牌>`。
**不要直接打开 file://…/public/index.html**：网页使用根路径脚本、样式和API，本地文件不是服务入口。

本地验收：

- /health返回200。
- /api/screen不带凭证返回401，带正确凭证返回200。
- 照片端点也需鉴权，不公开照片目录。
- Web显示虚构待办与时间；没有额度时是“--”，而不是编造100%。
- 打开网页只读，不改变周记内容。

### 4.2 建立公网HTTPS通路

本案例为常驻主机主动建立反向隧道，避免开放家庭路由器入站端口。
先在云服务器创建受限用户、确认主机密钥、配置只允许必要转发的专用SSH密钥；不要直接照抄root登录。

以下端口只是示例，必须检查本机是否占用：
```bash
ssh -N -T -o ExitOnForwardFailure=yes \
  -o ServerAliveInterval=30 -o ServerAliveCountMax=3 \
  -i /path/to/dedicated-tunnel-key \
  -R 127.0.0.1:18792:127.0.0.1:8787 tunnel-user@your-server
```

远端18792和本地8787均只绑定回环。公网只开放需要的HTTPS；不能把反向端口绑定0.0.0.0。
SSH命令测试通过后再交给守护程序；不要在命令行写密码或关闭StrictHostKeyChecking。

DNS将 board.example.com 指向自己的服务器。替换示例域名后，Nginx配置形如：
```nginx
# log_format放在http块，避免记录含view令牌的query string。
log_format board_safe '$remote_addr [$time_local] $request_method $uri $status';
server {
    listen 80;
    server_name board.example.com;
    return 301 https://$host$request_uri;
}
server {
    listen 443 ssl;
    server_name board.example.com;
    ssl_certificate /etc/nginx/ssl/board/fullchain.pem;
    ssl_certificate_key /etc/nginx/ssl/board/privkey.pem;
    access_log /var/log/nginx/board.access.log board_safe;
    client_max_body_size 256k;
    add_header Referrer-Policy no-referrer always;
    location / {
        proxy_pass http://127.0.0.1:18792;
        proxy_set_header Host $host;
        proxy_set_header X-Forwarded-Proto https;
    }
}
```

在既有服务器上合并配置，不覆盖其他站点。保存可恢复副本，执行nginx -t通过后才reload。
私钥只给服务需要的用户读取。检查错误日志/上游日志是否仍记录完整URL；安全access_log并不能自动净化所有日志。

验证DNS、证书主机名、有效期和完整链：
```bash
curl -I https://board.example.com/health
openssl s_client -connect board.example.com:443 \
  -servername board.example.com -showcerts </dev/null
```
浏览器能打开仅证明该浏览器信任链；仍要让AliGenie实际调用一次。
配置证书到期监测和续期负责人；换CA后确认旧续期任务不会覆盖新证书。

### 4.3 配置个人语音技能

1. 登录AliGenie技能平台，确认个人技能与屏显开发权限。
2. 创建个人技能，先只做一个无参数意图 `ShowBoard`。
3. 调用词用容易识别的中文，例如“我的工作台”。避免首次就用英语缩写或易混同音词。
4. 加入“打开我的工作台”等常用表达，设置默认/启动意图，完成训练。
5. 后端选“自建Web Service”，填 `https://board.example.com/webhooks/aligenie`。
6. 若平台要求域名认证文件，用平台原始文件名和内容配置 /aligenie/ 对应路由，必须逐字一致。
7. 本案例使用控制台自定义请求头 `X-Workboard-Key`；值为独立Webhook令牌。若当前账号没有该功能，先核对平台支持的鉴权协议，不要删掉后端鉴权来凑通。
8. 打开真机测试，选择账号下设备或语音验证码绑定，确认绑定完成。

在线测试先确认意图，再检查服务器有没有POST。普通浏览器地址栏是GET，直接打开Webhook看到Not found，不能据此判断POST失败。
不要把旧示例里的TodayFocus当作当前ShowBoard意图测试。

安全的本地手工协议检查（用你自己的令牌，不录像命令历史）：
```bash
curl -X POST http://127.0.0.1:8787/webhooks/aligenie \
  -H 'Content-Type: application/json' \
  -H 'X-Workboard-Key: <自己的Webhook令牌>' \
  --data '{"intentName":"ShowBoard","requestData":{}}'
```
示例在未配置模板标识时只播报，配置后返回屏显指令。HTTP200之后继续检查returnCode、executeCode和gwCommands。

### 4.4 单独做最小屏显，不要一开始接所有数据

在平台“屏显模板”中创建自己的名称、标识和说明，如 `MY_BOARD_V1`。
构建包在本地，标识在平台；后端响应引用的标识必须完全一致。
若要最小验证，可先把页面改成深色背景和白色“P01”文字，确认真机能看见再恢复完整界面。

本案例可用版本组合：

| 组件 | 版本 |
| --- | --- |
| waft | 0.7.51 |
| waft-cli | 0.7.32 |
| waft-web（本地网页容器） | 1.2.24 |
| waft-json | 1.1.10 |
| image-size | 2.0.4 |
| sharp | 0.35.4 |

这是复现基线，不是“最新推荐版本”。保留package-lock.json，先复现，再单独升级。
```bash
cd screen
npm ci --ignore-scripts
npm run build
node -e "const f=require('node:fs');if(!WebAssembly.validate(f.readFileSync('build/app.wasm')))process.exit(1)"
```
Waft CLI曾出现编译失败但退出码仍为0。检查日志无ERROR、产物确实更新，再验证WASM。
更进一步检查WebAssembly.Module.imports；不要默认新SDK生成的宿主导入在老固件存在。

本案例新屏显入口曾报HTTP500，官方样例也失败；后来通过独立模板的旧版页面完成上传。历史入口形如：
`https://iap.aligenie.com/console/newskill/0/tpl2-local?code=<自己的模板标识>`。
这只是本次记录，不是公开稳定API或权限绕过方式。必须先在自己的控制台创建模板；入口不可用时联系平台，不能无限重试或复制别人的标识。

上传WASM后，刷新历史包记录确认保存；进度100%不等于发布或设备已加载。按当前控制台支持的模板测试/发布流程操作，重新唤起技能看版本标记。不自动提交面向公众的技能发布。

### 4.5 Webhook返回正确屏显协议

设备实际跑通的是 `TPL.RenderTemplate`。不要与另一套 `TPL.RenderDocument` payload混合使用。
核心示意（实际可运行函数在example/src/screen.js）：

```js
{
  returnCode: "0", returnMessage: "success",
  returnValue: {
    reply: "正在打开工作台。",
    resultType: "RESULT", executeCode: "SUCCESS",
    gwCommands: [
      { commandDomain: "AliGenie.Speaker", commandName: "Speak",
        payload: { type: "text", text: "正在打开工作台。", expectSpeech: false } },
      { commandDomain: "AliGenie.Screen", commandName: "Render",
        payload: {
          pageType: "TPL.RenderTemplate", pageTitle: "个人工作台",
          data: { template: "MY_BOARD_V1",
            dataSource: { /* 首屏快照，以及带查看凭证的snapshotUrl */ } }
        } }
    ]
  }
}
```

本案例在线测试/部分请求缺少screenStatus。如果把“必须online”写成唯一门槛，可能只播报而不发Render。
最终路径只针对已绑定的带屏设备、ShowBoard意图和明确配置的模板发送Render；不要把强制渲染推广给所有无屏设备。
首屏把快照一起下发，设备再按snapshotUrl每分钟更新，减少初始化网络导致空白的概率。

### 4.6 接照片：比例、容量、字幕三件事分开做

读取照片的运行用户应能访问iCloud目录；另一台Mac用户名和目录可不同，使用环境变量而非复制开发笔记本绝对路径。
第一次先等照片下载到本地；服务日志只记录数量/错误类别，不输出私人文件全文。

本方案白名单：JPEG/PNG/WebP/GIF。HEIC未接入；不要因为手机照片可预览就假定设备引擎可解码。
GIF经缩略图处理不承诺动画。损坏/尚未下载/读不到尺寸的照片不下发。

图片不能为了填满640×360而强行拉伸：
```js
const scale = Math.min(640 / width, 360 / height);
const w = Math.round(width * scale), h = Math.round(height * scale);
const left = Math.round((640 - w) / 2), top = Math.round((360 - h) / 2);
```
先按EXIF旋转调整尺寸。横图、竖图、方图都要测试；有留边是保持原比例的正常结果。
Web用object-fit:contain；Waft实测需独立属性：
```xml
<image style="width:{{photoWidth}}px;height:{{photoHeight}}px;left:{{photoLeft}}px;top:{{photoTop}}px;" src="{{photoUrl}}" />
```
本项目整段style对象/字符串绑定曾导致图片消失，不能把浏览器CSS支持当作Waft原生支持。

服务端用sharp：
```js
sharp(file).rotate().resize(640, 360, {fit:'inside'})
  .flatten({background:'#080e14'})
  .jpeg({quality:85, progressive:false}).toBuffer();
```
只生成派生图，不改原图；缓存按文件修改时间和大小失效，限制容量。本案例抽检全量66张派生图均可解码、最大约80KB；原图有超过10MB的文件。
“原图大”是黑屏排查线索，不是已拿到设备崩溃日志证明的唯一根因。

轮换用洗牌队列而不是每次完全独立随机；跨轮避免连续同一张。必须考虑0张、1张、列表变化和下载失败。
Web版本会先预载再替换，失败保留上一张；设备版本当前直接换src，**原生端无完整预加载成功确认机制**，长时间轮换仍需验收。

字幕从周记图片引用关联：明确alt说明优先，否则使用对应周记标题。日期来自可信格式文件名或周记日期，并明确标注来源；不把mmexport时间当拍摄时间，也不声称AI逐图摘要。
当前索引仅扫描照片目录的父目录中的日期Markdown，并按文件basename匹配。建议同一年照片名称唯一；跨目录同名/跨年需要扩展索引键和扫描规则。

### 4.7 接周记待办

约定示例：
```markdown
# 2026年09月13日 每周回顾
## 下周待办
- **完成演示原型**：整理首页和数据入口
- [ ] 检查备份策略
- [x] 已经完成的事项
```

代码按文件名日期选择“不晚于今天”的最新周记，不按修改时间；跳过照片、附件和assets目录。
在该文件内寻找包含“下周”及“待办/方向/计划/事项”的标题，到同级或更高标题停止。去掉已勾选项，保留未完成条目；粗体开头作为短标题。
最新周记缺章节时提示缺失，不悄悄回退旧周。阅读结果缓存60秒。
当前规则是明确的Markdown约定，不是能理解任意周记格式的语义解析器。

屏显短标题最多19个字符，完整内容仍留在原文。固定顺序左1–4、中5–8、右9–12。
超过12项只显示前12项；由于后来移除标题，最终B15没有可见“还有更多”提示。这是已知边界，扩展时应选择分页或溢出提示，不宣称无限容量。

### 4.8 接Codex额度（可选模块）

官方App Server提供account/rateLimits/read；连接先initialize，再initialized通知，之后读额度。字段usedPercent是消耗，resetsAt是秒级时间戳；多桶返回优先选rateLimitsByLimitId中的codex。[官方协议](https://learn.chatgpt.com/docs/app-server)

本项目实现是本地只读JSON行协议：启动codex app-server，不创建线程、不启动模型、不兑换重置信用。
在primary/secondary里找windowDurationMins===10080的周窗口；缺失就报未知，不用五小时或模型专属桶替代。
余量clamp(100-usedPercent,0,100)，重置时间用北京时间展示。它不是现金余额，也不是订阅续费/会员到期日。

源代码模块example/src/codex-usage.js包含超时、结束子进程、去重采集和账户指纹校验。
首先让主机上的Codex自行登录；确认可执行文件的绝对路径。账户指纹仅用于本机校验，不把auth.json或登录token发给云端、设备、共享包。
文件式auth.tokens.account_id是本案例使用的本机存储形态；遇到新版本/其他登录模式字段不同，应按当前官方协议适配，不强行读取或打印全部凭据。
示例默认关闭采集；启用前填写CODEX_USAGE_BINARY和预期账户hash。生产中推荐用account/read进一步核对当前会话身份。

每5分钟更新成功时间；失败保留上次快照时间，不伪装为刚刚同步。15分钟以上过期显示--。
网页拿不到新快照也应停止把旧额度当实时；当前原生页断网保留旧屏，尚需额外过期UI，不能把保留旧屏称为实时。

### 4.9 守护服务与上线维护

Mac运行在拥有iCloud访问权的普通用户下，用LaunchAgent，不用root替代文件权限问题。
创建自己的plist；以下是模板，不可直接使用未替换的路径：
```xml
<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0"><dict>
<key>Label</key><string>com.example.workboard</string>
<key>ProgramArguments</key><array>
<string>/absolute/path/to/node</string><string>--env-file=.env</string><string>src/server.js</string>
</array>
<key>WorkingDirectory</key><string>/absolute/path/to/example</string>
<key>RunAtLoad</key><true/><key>KeepAlive</key><true/>
<key>StandardOutPath</key><string>/absolute/path/to/logs/workboard.log</string>
<key>StandardErrorPath</key><string>/absolute/path/to/logs/workboard.error.log</string>
</dict></plist>
```
先创建日志目录、设置.env权限600，plutil -lint检查后再launchctl bootstrap gui/用户UID。
隧道另设守护进程，使用完整SSH参数，测试用户注销/主机重启后的恢复条件。
LaunchAgent依赖用户会话，不自动等于无人登录开机可用。主机睡眠、iCloud未落地、网络断开要分别验收。
不要用“所有工具成功退出”代替真机测试。

## 5. 踩坑速查

| 现象 | 证据与可能原因 | 正确下一步 |
| --- | --- | --- |
| 上传一直网络异常 | 官方包也失败；本案例新入口HTTP500 | 看响应/保存记录，检查账号权限及支持入口，不反复改业务代码 |
| 是否平台已停服 | 公告针对在线开发/平台托管模块 | 区分模块下线和整个平台关闭；本例后续有自建服务成功证据 |
| 识别正常却“没有资源” | ASR/NLU正确不等于进入技能 | 查默认意图、测试开关、设备绑定、Webhook实际POST |
| 在线IDE_ERROR | 本例深层PKIX证书路径失败 | 查嵌套业务错误、TLS完整链，不盲改参数 |
| 浏览器HTTPS正常平台失败 | 信任库不同 | 从平台复测，必要时换兼容链；不关闭TLS验证 |
| 换RSA仍失败 | 算法与CA链是不同维度 | 不把RSA当通用修复，本例换另一链才成功 |
| 听到播报屏幕不变 | 未发送Render或错误门控 | 看gwCommands、模板标识、screenStatus缺失 |
| 屏幕白色 | 代码包/宿主导入/生命周期可能不兼容 | P01最小页、锁版本、检查导入和构建日志 |
| 调用随机函数后异常 | 本例需避免新增env.seed宿主导入 | 使用本地PRNG；随机只用于照片，不能生成安全token |
| 首屏有图后续不换 | 网络回包包装差异 | 兼容response/data/body/result/dataSource对象或JSON字符串 |
| 图像扁宽/拉伸 | 原生图片填充规则不同 | EXIF旋转+contain几何，逐属性绑定 |
| 新图黑屏 | 大图/格式/缓存/云盘未下载 | 查真实图片GET与解码，生成小JPEG，不先认定网络 |
| Web白屏 | 打开了file://文件 | 从HTTP(S)服务入口访问 |
| 普通域名无私人内容 | 缺少view凭证 | 用自己的完整私人链接；不要取消鉴权 |
| 余量与客户端不符 | 账号错、桶错、时间旧、单位错 | 校验账号、周窗口、时间戳秒转毫秒 |
| 新周待办不对 | 读取旧静态战略数据或按mtime挑选 | 改为最新日期周记和固定标题规则 |

## 6. 鉴权和脱敏：分享方案，不分享你的工作台

本例当前的view链接是Bearer式持有者凭证，不是绑定设备的身份。知道完整链接即可看数据。
查看令牌与Webhook令牌分开；以后写待办还要独立写权限，不能直接把只读链接升级为公网写权限。

录屏前：

- 切到虚构周记、无隐私测试图、明确标为模拟的额度或--。
- 隐藏地址栏query、书签、终端历史、环境变量、开发者工具请求头。
- 遮挡域名/IP、姓名、客户项目、账号头像、应用ID、设备UUID/MAC/SN、证书详情。
- 不展示私钥、证书ZIP、auth.json、完整traceId和请求体。
- 拍摄屏幕也要注意照片人物、反光中的人、桌面和周围文件。
- 误泄露令牌后需要撤销/轮换，不是事后打码就完成修复。

本公开包不含真实照片、周记、额度快照、内部部署日志、证书、密钥和node_modules。
只分享本包ZIP，不把整个私有项目或Git历史上传。
文档生成不代表已发送或公开发布；由你自行检查录像和发布渠道。

## 7. 验收清单

按顺序记录日期、版本、设备、结果；敏感标识留在自己的内部记录中。

1. 无凭证API/图片401，有凭证200；未知路径404；Webhook错误凭证401。
2. 同时确认HTTP状态和平台业务状态；能在日志看到对应时间的POST。
3. 真机连续多次唤起稳定；屏幕出现预期版本号。
4. 横图/竖图/方图与原图对照不变形；观察至少3次轮换，不只看首图。
5. 0张、1张、损坏图、缺失图、网络断开不会让整个页面崩溃。
6. 新周记/缺章节/已完成项/超过12项规则符合预期。
7. 用同一账户同一窗口对照额度；失败后更新时间不前移；过期不显示伪实时。
8. Web宽屏和手机窄屏无横向溢出；时间为预期时区。
9. 重启、息屏再唤起、隧道断线恢复分别实测。
10. 确认证书续期、权限、备份和日志保留策略。

不能承诺技能始终在前台：设备自己的首页、息屏、其他语音操作均可能打断。先测实际使用时长再讨论常驻方案。

## 8. 下一步：两端勾选待办（设计，未实现）

后端输出tasks数组：稳定id、title、done、version；身份来自周记标识和稳定任务标识，不能用第几行或截断标题。
独立存储完成状态，避免覆盖周记。写API提交明确done=true/false，不用容易重复翻转的toggle语义。
Web复选框与Waft触屏调用同一受写权限保护的API；响应成功才确认，失败回退；支持撤销、防连点和版本冲突。
完成后一端立即刷新，另一端轮询同步；验证换周、改名、重排、双端同时操作和重启持久化。
本次归档不实施此功能，旧board任务API也不等于周记任务勾选接口。

## 9. 资料与来源边界

- [AliGenie平台](https://iap.aligenie.com/home)
- [Waft介绍](https://aligenie.com/doc/20255408/aa9pvb)
- [技能屏显开发参考](https://aligenie.com/doc/20255408/nphxgg)
- [gwCommands参考](https://aligenie.com/doc/20255408/ehac4c)
- [在线开发下线公告](https://iap.aligenie.com/console/notice/12/detail)
- [Codex App Server官方文档](https://learn.chatgpt.com/docs/app-server)

2026-09-18重新检索时，OpenAI官方文档可读取；AliGenie部分页面在研究工具中返回错误，未获得新的正文验证。
阿里公告内容依据项目期间用户提供的控制台截图：2024-03-31下线在线开发/平台托管。不能把此信息扩大成当前整个技能平台停运。
AliGenie操作路径与错误经验来自本案例的控制台、源码和设备反馈，复现时以自己账号可见界面及最新官方说明为准。
公开包示例是教学起点：不包含一键购买、申请资质、绑定设备、部署证书或替你接受协议的自动化。

