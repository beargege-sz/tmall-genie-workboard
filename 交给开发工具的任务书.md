# 给 Codex / 其他开发工具的复现任务书
请先读同目录技术文档和 example 源码，不要直接照抄历史控制台地址或自动部署。

目标：做一套自有个人看板，常驻电脑维护数据，Web与天猫精灵Waft两端共享快照。
第一阶段仅展示：等比例照片、账户额度/重置时间、北京时间、最新周记的下周待办。
不得把API余额、ChatGPT额度、周窗口、模型专属窗口、订阅到期日混为一谈。

先向我收集：设备型号与固件、开发者权限和设备绑定方式、常驻主机系统、资料目录、公网域名/服务、是否允许照片经过公网、已有证书。
先在假数据模式工作。密钥由我在本地填写，不在聊天输出，不提交代码库。

里程碑：
1. 安装依赖，验证受保护的Web和空数据；缺凭证返回401。
2. 实现/验证公网HTTPS、最小语音Webhook、调用词和默认意图；确认真实平台POST抵达。
3. 上传最小文字P01 Waft模板，确认设备显示版本，不先接图片。
4. 使用无隐私测试图验证宽高比例、旋转与缩略图，再接只读照片目录。
5. 接入真实周记，确认选择日期、下周段落、最多12项、未找到时不静默回退。
6. 若需要Codex额度，按当前官方app-server文档校验协议、账号、周窗口和过期逻辑；不复制auth.json到云端。
7. Web和屏显统一API但分别实现UI。每一步区分本地、接口、平台、真机证据。
8. 配置守护、备份、日志脱敏、证书续期责任。输出我能复查的验收表。

遇到错误按层定位，最多重复一次相同失败。平台HTTP500时先用官方最小示例对照；不假定停服。
证书失败不能用关闭验证或HTTP替代。没有证据不能建议刷机、开放ADB或拆机。
不要发布公开技能、改DNS、开放防火墙、换证书或传私人照片，除非我明确授权对应动作。

扩展第二阶段（暂不实施，等我确认）：
跨端勾选待办：稳定taskId、独立持久状态、完成/撤销幂等写入、写权限与只读权限分离、防重复点击、断网提示、并发版本控制。
不能把截断标题或第几行当ID；不可覆盖原始周记。完成状态需跨刷新/重启保留，并验证新周不会串状态。

